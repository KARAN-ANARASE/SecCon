$ sudo apt install tor privoxy

configure pivoxy for TOR

$ sudo vim /etc/privoxy/config
search for = 127.0.0.1:9050 uncomment it
line n.o:1456
:wq

$ systemctl restart tor privoxy

tor take time for circute building.

Configure browser for privoxy
browser -> settings -> proxy -> manual ->
127.0.0.1 port = 8118
SOCKS5 = 127.0.0.1 Port: 8118

$ sudo systemctl restart tor privoxy

to set the entry and exit node restriction for TOR

$ sudo vim /etc/tor/torrc

go to end of the file add new entry

EntryNodes {pk},{sg},{de},{nl},{fr},{se}
ExitNodes {pk},{sg},{de},{nl},{fr},{se}
StrictNodes 1
MaxCircuitDirtiness 2
:wq

web hosting on dark-web
Darkweb TLD: Onion

$ sudo vim /etc/tor/torrc
search /HiddenService

HiddenServiceDir } uncomment it
HiddenServicePort } uncomment it

:wq

$ cd ~
$ mkdir -p darkwww
$cd darkwww
$ vim index.html
   <h1>This is dark web</h1>

$ sudo python3 -m http.server --bind 127.0.0.1 80

on new terminal
$ sudo systemctl restart tor

$ sudo su

$cd /var/lib/tor/hiddenservice$ cat hostname
--------.onion <- is hostname

on firefox open that hostname

Open your .onion domain in windows

https://www.torproject.org/download/

download browser from here
open tor->connect->enter domain in url->good to go.

+-#-+ Take SSH by using .onion SERVER setup +-#-+
**on tor servers kali machine**
$ sudo vim /etc/tor/torrc
HiddenServicePort 80 127.0.0.1:80
HiddenServicePort 22 127.0.0.1:22 }--- add this line for enable ssh through .onion

:wq

$ sudo systemctl restart ssh
$ sudo systemctl enable ssh
$ sudo systemctl restart tor

### on client any LINUX machine ###
$ sudo apt update
$ sudo apt install tor torsocks -y
$ sudo systemctl restart tor
$ sudo systemctl enable tor

$ torsocks ssh username@tordomain.onion 

### Take SSH by using .onion on client WINDOWS machine ###
[+] + R -> cmd
$ wsl --install
$ wsl
$ sudo apt update
$ sudo apt install tor torsocks -y
$ sudo systemctl restart tor
$ sudo systemctl enable tor

$ torsocks ssh username@tordomain.onion 

### Take RDP by using .onion SERVER setup ###
$ sudo systemctl install xrdp -y

$ sudo vim /etc/tor/torrc
HiddenServicePort 80 127.0.0.1:80
HiddenServicePort 22 127.0.0.1:22
HiddenServicePort 3389 127.0.0.1:3389 }--- add this line for enable RDP through .onion
:wq

$ sudo systemctl restart xrdp
$ sudo systemctl enable xrdp
$ sudo systemctl restart tor
$ sudo reboot }--- Now don't do login otherwise it is not able to create connection 

### Two ways to take RDP of kali on Windows ###
### 1. socat => Come on Windows machine   }--- good to go ---
[+] + R -> cmd 
$ wsl
$ sudo apt update -y && sudo apt upgrade -y
$ sudo apt install socat -y
$ socat TCP-LISTEN:13389,fork,reuseaddr SOCKS4A:127.0.0.1:tordomain.onion:3389,socksport=9050   }--keep this tab open now
[+] + R -> mstsc -> 127.0.0.1:13389 -> connect -> yes 

### 2. xfreerdp => come on Windows machine    
[+] + R -> cmd
$ wsl
$ sudo apt install freerdp3-x11 -y
$ torsocks xfreerdp /v:tordomain.onion:3389 /u:username   
